package com.example.easymart.presentation.theme.colors

import androidx.compose.ui.graphics.Color
// ==============================
// Brand / Primary colors
// Dùng cho:
// - nút chính
// - icon nổi bật
// - logo
// - link quan trọng
// - trạng thái active/focus
// ==============================

// Màu thương hiệu chính trên nền tối
val dark_primary = Color(0xFF7CC0FF)

// Màu chữ/icon trên nền primary
val dark_onPrimary = Color(0xFF00162E)

// Nền nhẹ hơn của primary, dùng cho chip, badge, card nhấn nhẹ, highlight nhóm
val dark_primaryContainer = Color(0xFF123252)

// Chữ/icon đặt trên primaryContainer
val dark_onPrimaryContainer = Color(0xFFD6EAFF)

// Gradient start cho các nút/banner nổi bật nếu cần
val dark_primaryGradientStart = Color(0xFF4F8DFF)

// Gradient end cho các nút/banner nổi bật nếu cần
val dark_primaryGradientEnd = Color(0xFF79B7FF)

// Màu viền/focus nhẹ liên quan primary
val dark_focusRing = Color(0xFF7CC0FF)

// ==============================
// Secondary / Tertiary colors
// Dùng cho:
// - phần tử phụ
// - nhấn nhá nhẹ
// - phân biệt nhóm nội dung khác
// - tag phụ, chip phụ, icon phụ
// ==============================

val dark_secondary = Color(0xFFB4C0D0)
val dark_onSecondary = Color(0xFF101C2B)
val dark_secondaryContainer = Color(0xFF273140)
val dark_onSecondaryContainer = Color(0xFFD7E3F8)

val dark_tertiary = Color(0xFFC0B2D1)
val dark_onTertiary = Color(0xFF241533)

// ==============================
// Semantic / Status colors
// Dùng cho:
// - validation
// - badge trạng thái
// - snackbar
// - thông báo hệ thống
// - trạng thái upload / warning / error / info
// ==============================

val dark_error = Color(0xFFFF6B6B)
val dark_onError = Color(0xFF2A0B0B)

val dark_success = Color(0xFF4ADE80)
val dark_onSuccess = Color(0xFF052E16)

val dark_warning = Color(0xFFFBBF24)
val dark_onWarning = Color(0xFF111111)

val dark_info = Color(0xFF60A5FA)
val dark_onInfo = Color(0xFF07111F)

val dark_successContainer = Color(0xFF14381F)
val dark_warningContainer = Color(0xFF3A2E10)
val dark_errorContainer = Color(0xFF3A1212)
val dark_infoContainer = Color(0xFF10233A)

// ==============================
// Background / Surface colors
// Dùng cho:
// - nền màn hình
// - card
// - sheet
// - bottom bar
// - vùng chứa nội dung
// ==============================

// Nền tổng của màn hình, thường dùng cho toàn app
val dark_background = Color(0xFF0B1220)

// Chữ chính trên nền background
val dark_onBackground = Color(0xFFF8FAFC)

// Nền card, sheet, bottom bar, vùng chứa chính
val dark_surface = Color(0xFF111827)

// Chữ chính trên surface
val dark_onSurface = Color(0xFFF8FAFC)

// Surface variant, dùng cho vùng nền phụ, section nền nhẹ, phân tách khối
val dark_surfaceVariant = Color(0xFF1F2937)

// Surface sáng hơn, dùng cho panel nổi bật nhẹ
val dark_surfaceBright = Color(0xFF172033)

// Surface tối hơn một chút, dùng cho nền nhấn nhẹ hoặc pressed area
val dark_surfaceDim = Color(0xFF0F172A)

// Nền container thấp, dùng cho khung input, vùng phụ, khối nested
val dark_surfaceContainerLow = Color(0xFF111827)

// Nền container chuẩn, dùng cho card, sheet, block nội dung chính
val dark_surfaceContainer = Color(0xFF111827)

// Nền container cao hơn, dùng cho vùng nested card, grouped card
val dark_surfaceContainerHigh = Color(0xFF1A2433)

// Nền container cao nhất, dùng cho vùng cần tách mạnh hơn một chút
val dark_surfaceContainerHighest = Color(0xFF223041)

// Màu scrim/overlay cho dialog, bottom sheet, modal
val dark_scrim = Color(0x99000000)

// ==============================
// Text / Icon colors
// Dùng cho:
// - tiêu đề
// - nội dung
// - placeholder
// - icon phụ
// - counter
// ==============================

val dark_textPrimary = Color(0xFFF8FAFC)
val dark_textSecondary = Color(0xFFCBD5E1)
val dark_onSurfaceVariant = Color(0xFF94A3B8)
val dark_iconMuted = Color(0xFF94A3B8)
val dark_onSelected = Color(0xFFD6EAFF)

// ==============================
// Border / Divider colors
// Dùng cho:
// - viền input
// - card border
// - divider
// - outline nhẹ
// ==============================

val dark_outline = Color(0xFF475569)
val dark_outlineVariant = Color(0xFF243244)
val dark_divider = Color(0xFF243244)

// ==============================
// Interaction / State colors
// Dùng cho:
// - pressed state
// - disabled state
// - selected state
// - focus
// ==============================

val dark_pressed = Color(0xFF1E293B)
val dark_selected = Color(0xFF17375B)
val dark_disabledContainer = Color(0xFF1F2937)
val dark_disabledContent = Color(0xFF64748B)

// ==============================
// AI / Special feature colors
// Dùng cho:
// - nút "Quét bằng AI"
// - banner AI
// - feature nổi bật mang tính riêng của EasyMart
// ==============================

val dark_aiAccent = Color(0xFF818CF8)
val dark_aiAccentLight = Color(0xFF1E1B4B)
val dark_aiAccentSoft = Color(0xFF312E81)