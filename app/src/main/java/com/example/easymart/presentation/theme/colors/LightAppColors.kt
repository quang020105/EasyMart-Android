package com.example.easymart.presentation.theme.colors

val LightAppColors = AppColors(
    // Brand
    primary = md_primary,
    onPrimary = md_onPrimary,
    primaryContainer = md_primaryContainer,
    onPrimaryContainer = md_onPrimaryContainer,
    primaryGradientStart = md_primaryGradientStart,
    primaryGradientEnd = md_primaryGradientEnd,
    focusRing = md_focusRing,

    // Secondary / Tertiary
    secondary = md_secondary,
    onSecondary = md_onSecondary,
    secondaryContainer = md_secondaryContainer,
    onSecondaryContainer = md_onSecondaryContainer,
    tertiary = md_tertiary,
    onTertiary = md_onTertiary,

    // Semantic / Status
    error = md_error,
    onError = md_onError,
    errorContainer = md_errorContainer,

    success = md_success,
    onSuccess = md_onSuccess,
    successContainer = md_successContainer,

    warning = md_warning,
    onWarning = md_onWarning,
    warningContainer = md_warningContainer,

    info = md_info,
    onInfo = md_onInfo,
    infoContainer = md_infoContainer,

    // Background / Surface
    background = md_background,
    onBackground = md_onBackground,

    surface = md_surface,
    onSurface = md_onSurface,

    surfaceVariant = md_surfaceVariant,

    surfaceBright = md_surfaceBright,
    surfaceDim = md_surfaceDim,

    surfaceContainerLowest = md_background,
    surfaceContainerLow = md_surfaceContainerLow,
    surfaceContainer = md_surfaceContainer,
    surfaceContainerHigh = md_surfaceContainerHigh,
    surfaceContainerHighest = md_surfaceContainerHighest,

    scrim = md_scrim,

    // Text / Icon
    textPrimary = md_textPrimary,
    textSecondary = md_textSecondary,
    onSurfaceVariant = md_onSurfaceVariant,
    iconMuted = md_iconMuted,
    onSelected = md_onSelected,

    // Border / Divider
    outline = md_outline,
    outlineVariant = md_outlineVariant,
    divider = md_divider,

    // Interaction / State
    pressed = md_pressed,
    selected = md_selected,

    disabledContainer = md_disabledContainer,
    disabledContent = md_disabledContent,

    // AI / Special Feature
    aiAccent = md_aiAccent,
    aiAccentLight = md_aiAccentLight,
    aiAccentSoft = md_aiAccentSoft
)