package com.example.easymart.presentation.theme.colors

val DarkAppColors = AppColors(

    // Brand
    primary = dark_primary,
    onPrimary = dark_onPrimary,
    primaryContainer = dark_primaryContainer,
    onPrimaryContainer = dark_onPrimaryContainer,
    primaryGradientStart = dark_primaryGradientStart,
    primaryGradientEnd = dark_primaryGradientEnd,
    focusRing = dark_focusRing,

    // Secondary / Tertiary
    secondary = dark_secondary,
    onSecondary = dark_onSecondary,
    secondaryContainer = dark_secondaryContainer,
    onSecondaryContainer = dark_onSecondaryContainer,
    tertiary = dark_tertiary,
    onTertiary = dark_onTertiary,

    // Semantic / Status
    error = dark_error,
    onError = dark_onError,
    errorContainer = dark_errorContainer,

    success = dark_success,
    onSuccess = dark_onSuccess,
    successContainer = dark_successContainer,

    warning = dark_warning,
    onWarning = dark_onWarning,
    warningContainer = dark_warningContainer,

    info = dark_info,
    onInfo = dark_onInfo,
    infoContainer = dark_infoContainer,

    // Background / Surface
    background = dark_background,
    onBackground = dark_onBackground,

    surface = dark_surface,
    onSurface = dark_onSurface,

    surfaceVariant = dark_surfaceVariant,

    surfaceBright = dark_surfaceBright,
    surfaceDim = dark_surfaceDim,

    surfaceContainerLowest = dark_background,
    surfaceContainerLow = dark_surfaceContainerLow,
    surfaceContainer = dark_surfaceContainer,
    surfaceContainerHigh = dark_surfaceContainerHigh,
    surfaceContainerHighest = dark_surfaceContainerHighest,

    scrim = dark_scrim,

    // Text / Icon
    textPrimary = dark_textPrimary,
    textSecondary = dark_textSecondary,
    onSurfaceVariant = dark_onSurfaceVariant,
    iconMuted = dark_iconMuted,
    onSelected = dark_onSelected,

    // Border / Divider
    outline = dark_outline,
    outlineVariant = dark_outlineVariant,
    divider = dark_divider,

    // Interaction / State
    pressed = dark_pressed,
    selected = dark_selected,

    disabledContainer = dark_disabledContainer,
    disabledContent = dark_disabledContent,

    // AI / Special Feature
    aiAccent = dark_aiAccent,
    aiAccentLight = dark_aiAccentLight,
    aiAccentSoft = dark_aiAccentSoft
)